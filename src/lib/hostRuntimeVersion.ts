/**
 * 编写器校验「最低 oclive 版本」时使用的宿主版本，应与 **oclivenewnew** `src-tauri/Cargo.toml` 的 `version` 一致。
 * 发布/升级 oclive 时请同步 bump（见 creator-docs `PACK_VERSIONING.md`）。
 */
export const HOST_RUNTIME_VERSION = '0.5.0'
