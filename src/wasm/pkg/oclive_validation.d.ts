/** 与 `oclive_validation.js` 配套；`wasm-pack` 覆盖 JS/wasm 后类型可能由生成物更新。 */
export default function init(moduleOrPath?: unknown): Promise<void>
export function validateManifestWasm(
  mergedManifestJson: string,
  mergedSceneIdsJson: string,
  hostRuntimeVersion: string,
): void
